// Sync compilation (default)

const Module = require('arima/wasm/native-sync.js')
const bin = require('arima/wrapper/native.bin.js')
const loadARIMA = require('arima/load.js')

const moduleObject = Module({ wasmBinary: bin })

module.exports = loadARIMA(moduleObject)
